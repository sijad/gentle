# helps
scalar YesNo

schema {
  query: Query
}

type EchoPayload {
  result: String!
}

type Query {
    randomNumber(id: Int!): Int
    randomYesOrNo(id: Int!): YesNo!
    echo(txt: String!): EchoPayload!
}
